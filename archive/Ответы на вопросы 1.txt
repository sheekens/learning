1. Какие три основных типа данных используются в компьютере для представления величин реального мира? Привести один-два предмета или его свойства, представленного с помощью каждого из типа данных.

str - строка, текстовые данные (названия книг о расизме, сортов дерьма)
int - целочисленные данные (подсчёт людей, бананов, светофоров)
float - числа с плававающей точкой (скорость, масса, длина)

2. Что делает функция type()?

возвращает тип (класс) данных

3. Что делает функция len()?

возвращает длину str

4. Какое ключевое слово и какой символ используется для вставки переменной в указанное место строки?

'{}'.format()

5. Одинаковые ли строки '2023-03-11' и '2023-03-11 '?

разная длина. в переменных времени второй вариант может не сработать

6. Ко всем ли типам данных можно обращаться с помощью индекса (операции []) и функции len()?

'int', 'float' object is not subscriptable
'int', 'float' no len

7. Привести пример условия if из реальной жизни, в котором есть несколько вариантов elif и есть else

if vyezjayu_iz_doma <= '07:45':
	rabota_arrival = '08:00'
elif vyezjayu_iz_doma > '07:45' and < '08:45':
	rabota_arrival = 'pozdno, brat'
else:
	rabota_arrival = 'ne segodnya, brat'

8. Как проверить наличие симлова в строке или списке?

print(symbol in stroka) символ должен быть str
print(symbol in list) символ должен быть того же типа данных, что и данные в списке

9. Методы keys(), values(), items() словаря

keys() - обращается ко всем ключам словаря
values() - обращается ко всем значениям словаря
items() - обращается ко всем парам ключ-значение словаря

10. Как добавить новый ключ в словарь?

words_dict = {'welcome': 1, 'to': 999 }
words_dict['from'] = 10